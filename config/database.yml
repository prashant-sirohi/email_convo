# Postgres
#   gem install pg
#
#   Ensure the pg gem is defined in your Gemfile
#   gem 'pg'
#
default: &default
  adapter: postgresql
  encoding: unicode
  pool: 5
  timeout: 5000
  reconnect: false
  host: localhost
  port: 5432

development:
  adapter: postgresql
  database: email_convo
  username: postgres
  password: test